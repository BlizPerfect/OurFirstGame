using System;
using System.Drawing;

class Gate
{
    public Point GatePosition;
    public int GateID;
    public Gate(Point gatePosition, int gateId)
    {
        GatePosition = gatePosition;
        GateID = gateId;
    }
}
